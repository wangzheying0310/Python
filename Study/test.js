var msg = 'Hello World';
console.log(msg);

// 这个函数将两个数字相加并返回他们的和
function addNumbers(num1, num2) {
    return num1 + num2;
  }
  
  // 调用addNumbers函数并存储结果在变量sum中
  const sum = addNumbers(3, 5);
  
  // 打印出sum的值
  console.log(sum);